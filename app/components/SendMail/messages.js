/*
 * SendMail Messages
 *
 * This contains all the text for the SendMail component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SendMail';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SendMail component!',
  },
});
